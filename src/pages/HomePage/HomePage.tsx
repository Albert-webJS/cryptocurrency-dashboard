import { Header } from "src/layout";
import { Wrapper } from "src/shared";


export const HomePage = (): JSX.Element => {
    return (
        <Wrapper>
            <Header />
        </Wrapper>
    )
}

